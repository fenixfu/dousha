# Doubao-first Windows MVP

The first usable Windows version will ship the Doubao dictation path rather than substituting an easier generic cloud ASR. This preserves Dousha's product identity, even though it requires a Windows-compatible Doubao audio encoding and transport adapter instead of directly reusing the macOS `AVFoundation` / `AudioToolbox` implementation.
