---
status: accepted
---

# Defer the long-term Windows implementation selection

The .NET and Swift Windows implementations remain parallel evaluation vehicles while the project compares Doubao Text Post-Processing, incremental LLM Dictation Post-Processing, and their hybrid. The long-term Windows implementation will be selected only after that strategy evaluation, using the owner's lived dictation experience rather than treating the implementation language as the primary performance variable.
