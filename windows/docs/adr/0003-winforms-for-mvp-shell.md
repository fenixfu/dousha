# WinForms for the MVP shell

The Windows MVP will use WinForms for the tray shell and settings window. The first release needs a notification-area presence, menu commands, and a small configuration surface; WinForms supports those directly and keeps attention on the higher-risk dictation loop, while richer UI frameworks can be revisited after the Doubao path is stable.
